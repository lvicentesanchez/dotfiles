alias ls="ls --color=auto"
alias l="ls -la"

eval (dircolors -c $HOME/.dir_colors/dircolors)

set -gx PATH ~/.local/bin $PATH

# OPAM configuration
. /home/luisvicente/.opam/opam-init/init.fish > /dev/null 2> /dev/null or true
